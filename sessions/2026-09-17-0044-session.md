# Session handoff — 2026-09-17 00:44 MSK

## Outcome

T1-0's active 0–18 minute slice was rebuilt around real storage gates. The
next discovery can exceed the current resource cap, requiring a repeatable
storage purchase. Discoveries no longer grant capacity.

The local development build is reachable through the tailnet:

`https://localwsl.tail404046.ts.net/`

## Implemented

- Ruleset `timeline1-v7-storage-gates`.
- Base caps: RNA 100, DNA 100, Biomass 80, ATP 40, Food 300, Materials 150,
  Knowledge 100, Power 100.
- Capacity comes only from buildings. Main early gates:
  - M02 (130 RNA) requires Membrane Layers (`+250 RNA` each).
  - M06 (205 DNA) requires Genetic Storage (`+150 DNA`).
  - C03 (110 Biomass, 50 ATP) requires Biomass Reserve (`+160 Biomass`) and
    ATP Reserve (`+120 ATP`).
  - T02 and T05 have Food/Materials/Knowledge storage gates prepared before
    their respective costs exceed base caps.
- Molecular flow follows legacy direction: DNA Synthesis consumes 0.52 RNA/s
  for 0.26 DNA/s. ATP production consumes 0.30 Biomass/s for 0.35 ATP/s.
- Biomass is environmental uptake, not an RNA/DNA conversion.
- The event-aware headless simulator now purchases required storage structures.
  Its competent route buys four Membrane Layers plus Genetic, Biomass and ATP
  stores, reaches M06 at 09:55 and C06 at 18:00; optimized reaches C06 at
  15:27 (seed 7).
- Save migration v7 removes historical node capacity modifiers, restores cap
  effects from existing building counts, and clamps stored resources to the
  resulting caps.
- UI includes the Biomass Reserve display name; resource counters/ETAs remain
  integer-facing.

## Important boundary for next chat

Do not claim the entire later economy is restored yet. Food has a basic job
output, but live Materials, Knowledge and Power production/consumption loops
are not implemented. Their caps, stores and future gating config exist only to
avoid dead ends. The next work should reconstruct original resource sources,
consumers, deficits and storage semantics for T1-3 onward before balancing
those phases.

For the original legacy reference already inspected this session:

- the molecular loop begins with RNA/DNA caps of 100;
- membrane actions add RNA capacity; eukaryotic-cell actions add DNA capacity;
- DNA costs RNA at a 2:1 ratio;
- legacy resources beyond this molecular portion are not a justification for
  inventing unconsumed replacement resources.

## Checks completed

- `npm test`
- `npm run test:sim`
- `npm run smoke`
- `npm run build`
- `git diff --check`
- Tailnet HTTP check returned `200` and served the v7 bundle.

## Workspace state

No commit was created in this session. The worktree is intentionally dirty and
includes unrelated pre-existing documentation/layout changes. Preserve them;
do not reset or bulk-revert the tree.

Primary files changed for the storage pass:

- `src/chronicles/config/resources.js`
- `src/chronicles/config/buildings.js`
- `src/chronicles/config/nodes.js`
- `src/chronicles/config/producers.js`
- `src/chronicles/dev/simulation.js`
- `src/chronicles/save/migrations.js`
- `src/chronicles/ui/app.js`
- `tests/config/spec.js`, `tests/domain/spec.js`, `tests/save/spec.js`,
  `tests/simulation/spec.js`
- `docs/gdd/02_ECONOMY_FIRST_120_MINUTES.md`
- `docs/PROJECT_STATE.yaml`, `docs/TODO.md`

## Next actions

1. Start from this handoff and manually playtest the v7 0–18 path through the
   tailnet build.
2. Reconstruct the original resource chains from T1-3 onward, resource by
   resource: source, consumption, cap, storage, and failure/recovery behavior.
3. Implement these chains before adding their balancing simulation profiles.
4. Re-run full test, simulation, smoke and build gates after each phase.
